when:
  - event: cron
  - event: manual

steps:
  renovate:
    image: renovate/renovate:latest
    environment:
      RENOVATE_TOKEN:
        from_secret: renovate_token
      RENOVATE_CONFIG_FILE: renovate/config.js
      LOG_LEVEL: info
    commands:
      - renovate
